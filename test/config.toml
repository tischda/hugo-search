baseUrl = "http://localhost/"

title = "hugo-search test site"
pluralizelisttitles = "false"

theme = "basic"

[params]
    # Default search URL (no trailing slash!)
    searchUrl = "http://localhost:8080"
